const express = require("express");
const app = express();

const port = process.env.port || 3000;
app.get("/", (req, res) => {
	res.send(`<div>
	<h2>Welcome to anil neerukonda institute of technology and sciences</h2>
	<p>This is g.varshith from cse-c</p>
    
</div>`);
});
app.listen(port, () => {
	console.log(`Listening to port ${port}`);
});
